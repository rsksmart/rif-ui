import { FC } from 'react';
import { TableProps as MUITableProps } from '@material-ui/core';
export interface TableProps extends MUITableProps {
}
declare const Table: FC<TableProps>;
export default Table;
