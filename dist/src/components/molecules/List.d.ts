import { FC } from 'react';
import { ListProps as MUIListProps } from '@material-ui/core';
export interface ListProps extends MUIListProps {
}
declare const List: FC<ListProps>;
export default List;
