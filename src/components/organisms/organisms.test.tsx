import { Account, Footer } from '.'

describe('Account component sanity', () => {
  it('is truthy', () => {
    expect(Account).toBeTruthy()
  })
})

describe('Footer component sanity', () => {
  it('is truthy', () => {
    expect(Footer).toBeTruthy()
  })
})
